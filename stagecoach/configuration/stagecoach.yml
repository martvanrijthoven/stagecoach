stagecoach:
  default:
    SKIPPABLE: False

    stages!required:
    trails: None

    stage_coach#stagecoach:
      output_folder: ${OUTPUTS:output_folder.path}
      stages: ${stages}
      trails: ${trails}