train:
  default:
    output_folder: ${OUTPUTS:output_folder.path}/adam/training
    optimizer#dontbuild:
      "*object": torch.optim.Adam
inference:
  default:
    output_folder: ${OUTPUTS:output_folder.path}/adam/inference
report:
  default:
    output_folder: ${OUTPUTS:output_folder.path}/adam/report